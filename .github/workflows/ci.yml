name: CI

on:
  push:
  pull_request:

jobs:
  doc-invariants:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check document invariants
        run: bash tests/check-docs.sh
