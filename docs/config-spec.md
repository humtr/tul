# tul Config Spec

`tul` separates configuration into three layers:

1. Global config: platform paths and project aliases.
2. Repo `.tul.yml`: repo identity, expected branch, checks, track.
3. Package `tul-package.yml`: target, apply files, commit files/message.
