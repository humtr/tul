# windows dwork environment

This file is a retained compatibility pointer.

Canonical environment-profile notes now live in:

```text
docs/environments/README.md
```

The Windows `D:\work` profile is now treated as one environment profile under the broader environment-profile namespace, not as a standalone active root document.

Keep this pointer only to avoid breaking existing external links. New edits should go to `docs/environments/README.md`.
